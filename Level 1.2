#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Iki eded daxil edin: ";
    cin >> a >> b;

    cout << "Cem: " << a + b << endl;
    cout << "Ferq: " << a - b << endl;
    cout << "Hasil: " << a * b << endl;
    cout << "Bolme: " << (float)a / b << endl;

    return 0;
}
